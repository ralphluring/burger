    <div class="uneaten">   
        <div class="devourit">
            <h5><span>{{burger_id}}</span> : {{burger_name}} </h5>
            <button type="submit" class="devour-btn" data-id="{{burger_id}}">devour</button>
        </div>  
    </div>